package com.finsights.portfolio.domain;

/** Computed from the three scenario-question answers in persona onboarding — not directly
 *  user-chosen. See PersonaService for the scoring. */
public enum RiskProfile { CONSERVATIVE, MODERATE, AGGRESSIVE }
